
# Dependencias recomendadas y comando único

Ejecutar en la raíz del proyecto:

```bash
npm install
npm install -D @tailwindcss/postcss
```

Luego validar:

```bash
npm run build
```

Si falla por caché en Netlify, limpiar caché y redeploy.
